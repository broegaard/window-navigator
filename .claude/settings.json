{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty') && echo \"$f\" | grep -qE '\\.py$' || exit 0; ROOT=$(git rev-parse --show-toplevel 2>/dev/null || echo \"$PWD\"); export PATH=\"$ROOT/.venv/bin:$PATH\"; make -C \"$ROOT\" lint; make -C \"$ROOT\" test; true",
            "timeout": 120,
            "statusMessage": "Linting and testing..."
          }
        ]
      }
    ]
  }
}
